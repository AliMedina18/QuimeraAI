# 🔍 AUDITORÍA DE ESTRUCTURA Y USO DEL CÓDIGO — QuimeraAI

**Fecha:** 2026-06-07  
**Objetivo:** Identificar código muerto, dependencias sin usar, y oportunidades de limpieza mientras se mantiene una estructura sólida.

---

## 📊 RESUMEN EJECUTIVO

| Categoría | Estado | Acción |
|-----------|--------|--------|
| **Backend** | ⚠️ Parcialmente utilizado | Ver sección Backend |
| **Frontend** | ✅ Bien estructurado | Verificar imports |
| **Dependencias** | ⚠️ Parcialmente utilizadas | Ver sección Dependencias |
| **Código muerto** | 🟡 Detectado | Ver lista detallada |

---

## 🔴 BACKEND — Análisis Detallado

### ✅ LO QUE SE USA

#### 1. **main.py** — Endpoints activos y funcionales
```
GET  /health                         ✅ Funcional — Cloud Run health check
GET  /gemini-test                    ✅ Funcional — Verifica Gemini
POST /analyze                        ✅ Funcional — Paso 1 (análisis)
POST /evaluate                       ✅ Funcional — Paso 1+2 (loop corrección)
POST /generar-diseno                 ✅ Funcional — Pipeline completo (SSE)
GET  /proyecto/{id}                  ✅ Stub presente (no se llama)
```

#### 2. **models.py** — Modelos de datos
```
✅ DesignContext            — Central, fluye por todo el pipeline
✅ DesignRequest            — Payload entrada endpoints
✅ HealthResponse           — /health
✅ GeminiTestResponse       — /gemini-test
✅ AestheticScores          — Resultados evaluación
❓ DesignProposal           — Definido pero nunca instanciado (ver después)
```

#### 3. **services/gemini_client.py** — Gemini SDK
```
✅ GeminiClient             — Usado en step1, step2, step3
✅ generate_text()          — Llamado en Paso 1, 2 y 3
✅ models "pro" y "flash"   — Correctos
```

#### 4. **services/firestore_client.py** — Guardado de resultados
```
✅ FirestoreClient          — Instanciado en main.py (líneas 348, 416)
✅ save_generated_output()  — Llamado en endpoint /generar-diseno
✅ save_aesthetic_scores()  — **NO SE LLAMA** ⚠️
✅ get_project()            — **NO SE LLAMA** ⚠️
```

#### 5. **services/storage_client.py** — Cloud Storage
```
✅ StorageClient            — Instanciado en main.py (línea 340)
✅ upload_component()       — Llamado en /generar-diseno
✅ upload_tokens()          — Llamado en /generar-diseno
✅ upload_rationale()       — Llamado en /generar-diseno
```

#### 6. **pipeline/step1_analyze.py** — Paso 1
```
✅ analyze_and_design()     — Llamado en /analyze y /evaluate
✅ generate_harmonic_palette() — Importado pero **NO SE USA** ⚠️
```

#### 7. **pipeline/step2_evaluate.py** — Paso 2
```
✅ aesthetic_evaluate()             — Llamado en /evaluate y /generar-diseno
✅ _run_algorithmic_scorers()       — Llamado dentro de aesthetic_evaluate()
✅ _run_llm_scorers()               — Llamado dentro de aesthetic_evaluate()
✅ score_color_harmony()            — Usado en scorers algoritémicos
✅ score_wcag_contrast()            — Usado en scorers algoritémicos
✅ Scorers LLM (6 funciones)        — Usados en paralelo con asyncio.gather()
```

#### 8. **pipeline/step3_generate.py** — Paso 3
```
✅ generate_code()         — Llamado en /generar-diseno
✅ _build_generate_prompt() — Helper usado internamente
✅ _validate_component_jsx() — Validación básica de JSX
```

#### 9. **pipeline/scorers/**
```
✅ wcag_contrast.py        — Totalmente funcional y probado
✅ color_harmony.py        — Funcional, usadas sus funciones
✅ llm_scorers.py          — 6 scorers, todos llamados en paralelo
```

### ❌ LO QUE NO SE USA

#### 1. **Función muerta en services/firestore_client.py**
```python
# ❌ save_aesthetic_scores()  — Nunca se llama
# ❌ get_project()             — Nunca se llama
```
**Impacto:** Bajo. Son helpers para futuro. El endpoint `/proyecto/{id}` que las usaría está stub.

#### 2. **Endpoint stub en main.py**
```python
@app.get("/proyecto/{id}", tags=["Almacenamiento"])
async def get_proyecto(id: str):
    """Recupera un proyecto guardado de Firestore."""
```
**Estado:** Stub completo, no implementado.  
**Impacto:** Bajo en MVP, pero útil para v2.

#### 3. **Import sin usar en step1_analyze.py**
```python
from pipeline.scorers.color_harmony import generate_harmonic_palette
```
**Uso:** Se importa pero **nunca se llama**.  
**Impacto:** Bajo. Es un helper que podría usarse.

#### 4. **Posibles variables sin usar**
```python
# En models.py:
class DesignProposal(BaseModel):  # ❓ Definida pero ¿usada?
```

### 📦 Dependencias Backend (requirements.txt)

#### Usadas ✅
```
google-genai==1.16.0                    ✅ Paso 1, 2, 3
google-cloud-firestore==2.19.0          ✅ Guardado de resultados
google-cloud-storage==2.18.0            ✅ Upload de archivos
google-cloud-secret-manager==2.20.0     ❓ Importada pero no usada en código
fastapi==0.115.0                        ✅ API
uvicorn==0.32.0                         ✅ Servidor
pydantic==2.9.0                         ✅ Validación
python-dotenv==1.0.1                    ✅ Env vars
colour-science==0.4.4                   ✅ Color harmony (OKLCH)
colorspacious==1.1.2                    ⚠️ No se usa realmente (colour-science lo reemplaza)
pytest==8.3.0                           ✅ Testing
pytest-asyncio==0.24.0                  ✅ Testing async
httpx==0.28.1                           ✅ Testing HTTP
```

#### Sin usar ❌
```
google-cloud-secret-manager==2.20.0     ❓ Importada pero NO llamada en código real
                                           (Secret Manager es accedido por Cloud Run)
colorspacious==1.1.2                    ⚠️ Redundante, colour-science lo cubre
```

---

## 🎨 FRONTEND — Análisis Detallado

### ✅ LO QUE SE USA

#### 1. **Componentes cargados en page.tsx**
```
✅ ChatUI            — Input del usuario, panel izquierdo
✅ Scorecard         — Scores de evaluación (📊 tab)
✅ Preview           — Preview React (🖥 tab)
✅ CodeView          — Código generado ({ } tab)
✅ RationaleView     — Rationale (📝 tab)
```

#### 2. **Hooks personalizados**
```
✅ usePipeline()     — Único hook, gestiona todo el estado SSE + eventos
```

#### 3. **Tipos**
```
✅ pipeline.ts       — Todos los tipos se usan
  - PipelineStatus
  - PipelineStep
  - AestheticScores
  - DesignProposal
  - SSEEvent
  - PipelineState
  - CRITERIA_LABELS
```

#### 4. **Estilos**
```
✅ globals.css       — Tailwind, reseteos
✅ Scorecard.css     — Estilos específicos para scorecard
✅ tailwind.config.ts — Configuración Tailwind
```

#### 5. **Dependencias npm**
```
✅ next              — Framework SSR/SSG
✅ react             — UI
✅ react-markdown    — Renderizado markdown (rationale document)
✅ react-syntax-highlighter — Resaltado código (CodeView)
```

### ❌ LO QUE NO SE USA

#### 1. **Componentes CSS sin usar**
```
✅ Scorecard.css     — Tiene estilos definidos pero parecen estar reflejados en 
                        el componente inline
```
**Impacto:** Bajo. CSS comprimido en prod.

#### 2. **Tipos nunca instanciados**
```
❓ DesignProposal    — Tipo definido, usado en pipeline.ts pero 
                        ¿realmente se instancia en runtime?
```

---

## 🔧 DEPENDENCIAS — Consolidado

### Backend
| Package | Version | Status | Notas |
|---------|---------|--------|-------|
| google-genai | 1.16.0 | ✅ | Gemini SDK |
| google-cloud-firestore | 2.19.0 | ✅ | Guardado |
| google-cloud-storage | 2.18.0 | ✅ | Upload |
| google-cloud-secret-manager | 2.20.0 | ⚠️ | Importada pero no usada |
| fastapi | 0.115.0 | ✅ | API |
| uvicorn | 0.32.0 | ✅ | Servidor |
| pydantic | 2.9.0 | ✅ | Validación |
| python-dotenv | 1.0.1 | ✅ | Config |
| colour-science | 0.4.4 | ✅ | Color harmony |
| colorspacious | 1.1.2 | ⚠️ | Redundante |
| pytest | 8.3.0 | ✅ | Testing |
| pytest-asyncio | 0.24.0 | ✅ | Testing |
| httpx | 0.28.1 | ✅ | Testing |

### Frontend
| Package | Version | Status | Notas |
|---------|---------|--------|-------|
| next | ^15.0.0 | ✅ | Framework |
| react | ^18.3.0 | ✅ | UI |
| react-dom | ^18.3.0 | ✅ | UI rendering |
| react-markdown | ^9.0.1 | ✅ | Rationale docs |
| react-syntax-highlighter | ^15.5.0 | ✅ | Code preview |

---

## 📝 CÓDIGO MUERTO / INNECESARIO

### Nivel ALTO (Eliminar AHORA)
```
❌ backend/services/firestore_client.py:
   - save_aesthetic_scores()  (línea ~73)
   - get_project()            (línea ~89)
   
   ➜ Razón: No se llaman. El endpoint /proyecto/{id} que las usaría es stub.
   ➜ Acción: Eliminarlas O esperar a v2 con endpoint completo.

❌ backend/main.py:
   - GET /proyecto/{id}       (línea ~416)
   
   ➜ Razón: Endpoint completamente stub.
   ➜ Acción: Eliminar o implementar completamente.
```

### Nivel MEDIO (Limpiar / Optimizar)
```
⚠️ backend/pipeline/step1_analyze.py:
   - Import: from pipeline.scorers.color_harmony import generate_harmonic_palette
   
   ➜ Razón: Se importa pero nunca se llama.
   ➜ Acción: Eliminar import o usarla en el score.

⚠️ backend/requirements.txt:
   - google-cloud-secret-manager==2.20.0
   - colorspacious==1.1.2
   
   ➜ Razón: No se usan en código directo (Secret Manager es cloud-run-native).
   ➜ Acción: Eliminar de requirements, validar que funciona sin ellas.
```

### Nivel BAJO (Documentar / Aceptar)
```
ℹ️ backend/models.py:
   - class DesignProposal(BaseModel)
   
   ➜ Razón: Modelado futuro, no afecta rendimiento.
   ➜ Acción: Dejar por claridad arquitectónica.

ℹ️ frontend/components/Scorecard.css
   
   ➜ Razón: Estilos separados que probablemente dupliquen Tailwind inline.
   ➜ Acción: Consolidar en componente TSX o dejar si tiene valor de reutilización.
```

---

## 🏗️ RECOMENDACIONES ESTRUCTURALES

### Backend

#### 1. **Limpieza inmediata**
```bash
# Eliminar o completar:
# 1. Endpoint /proyecto/{id} (stub)
# 2. Métodos firestore sin usar (save_aesthetic_scores, get_project)
# 3. Import sin usar (generate_harmonic_palette)
```

#### 2. **Optimizar imports en step1_analyze.py**
```python
# ❌ Actual
from pipeline.scorers.color_harmony import generate_harmonic_palette

# ✅ Propuesto: Si no se usa, eliminar
# Si se debe usar, llamar en el código
```

#### 3. **requirements.txt — Limpiar dependencias**
```
# ❌ Mantener
google-cloud-secret-manager==2.20.0  # Cloud Run maneja esto nativamente

# ❌ Verificar si se necesita
colorspacious==1.1.2  # colour-science probablemente lo reemplaza
```

#### 4. **Estructura de carpetas — Sugerencia**
```
backend/
├── main.py                  ✅ Entrada FastAPI
├── models.py               ✅ Pydantic models
├── config.py               🔶 Crear: centralizar configs y env vars
├── services/
│   ├── gemini_client.py    ✅
│   ├── firestore_client.py ✅ (Limpiar métodos sin usar)
│   └── storage_client.py   ✅
├── pipeline/
│   ├── step1_analyze.py    ✅ (Limpiar imports)
│   ├── step2_evaluate.py   ✅
│   ├── step3_generate.py   ✅
│   └── scorers/
│       ├── wcag_contrast.py ✅
│       ├── color_harmony.py ✅
│       └── llm_scorers.py   ✅
└── tests/                   ✅
```

### Frontend

#### 1. **Consolidar estilos**
```
frontend/
├── app/
│   ├── globals.css        ✅ Tailwind + reseteos
│   └── page.tsx           ✅
├── components/
│   ├── ChatUI.tsx         ✅
│   ├── Scorecard.tsx      ✅
│   ├── Scorecard.css      🔶 Revisar si es redundante
│   ├── Preview.tsx        ✅
│   ├── CodeView.tsx       ✅
│   └── RationaleView.tsx  ✅
├── hooks/
│   └── usePipeline.ts     ✅
└── types/
    └── pipeline.ts        ✅
```

#### 2. **Optimizaciones**
```
- Revisar si Scorecard.css puede migrarse a Tailwind/CSS-in-JS
- Verificar que react-markdown y react-syntax-highlighter tienen tamaño justificado
- Considerar lazy-load de CodeView si el bundle crece
```

---

## 📊 ESTADO RESUMIDO

| Aspecto | Calificación | Comentario |
|---------|-------------|-----------|
| **Backend estructura** | 8/10 | Bien organizado, limpiar Firestore stubs |
| **Frontend estructura** | 9/10 | Excelente. Consolidar CSS. |
| **Dependencias** | 7/10 | Algunos packages redundantes |
| **Código muerto** | 6/10 | Presente pero bajo impacto |
| **Mantenibilidad** | 8/10 | Buena, mejorable con limpieza |

---

## ✅ CHECKLIST DE ACCIONES RECOMENDADAS

### AHORA (P0 — Crítico)
- [ ] Eliminar endpoint stub `/proyecto/{id}` de main.py
- [ ] Eliminar métodos Firestore no usados (save_aesthetic_scores, get_project)
- [ ] Eliminar import `generate_harmonic_palette` de step1_analyze.py

### PRONTO (P1 — Alto)
- [ ] Eliminar `google-cloud-secret-manager` de requirements.txt y validar
- [ ] Eliminar `colorspacious` de requirements.txt y validar
- [ ] Revisar Scorecard.css — consolidar en TSX o documentar reutilización

### MÁS TARDE (P2 — Bajo)
- [ ] Crear `backend/config.py` para centralizar configuración
- [ ] Documentar `DesignProposal` — ¿realmente se necesita?
- [ ] Audit de bundle size frontend (react-markdown, react-syntax-highlighter)

---

## 🎯 CONCLUSIÓN

**QuimeraAI tiene una estructura sólida.**

✅ **Lo bueno:**
- Pipeline bien diseñado y separado en pasos
- Código limpio con logging descriptivo
- Frontend componentizado correctamente
- Tipos TypeScript presentes

⚠️ **Lo a mejorar:**
- Código stub que no debería estar en main branch
- Algunos métodos y imports muertos
- Dependencias redundantes
- Oportunidad de consolidar estilos

**Recomendación:** Realizar limpieza P0 antes de pasar a Día 2/3 del desarrollo. La estructura soporta bien el crecimiento futuro, pero necesita un poco de higiene.

---

**Generado:** 2026-06-07 | **Auditoría:** Completa
