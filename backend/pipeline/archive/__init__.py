# archive/ — Pipeline de Evaluación Estética v1 (no activo)
#
# Estos módulos pertenecen al diseño original de Quimera donde el pipeline
# era: analyze → evaluate (8 criterios) → generate.
#
# El pipeline activo (v3) evolucionó a: templates → analyze → images → generate.
# Los modelos de DesignContext cambiaron y estos archivos quedaron desconectados.
#
# Se conservan aquí como referencia y posible base para una futura
# integración de evaluación con el nuevo DesignContext.
